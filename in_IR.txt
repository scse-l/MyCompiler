const pi = 3, e = 2,target = 'g',c = '5';

var left,right,mid,n,i,j:integer;name:array[30] of integer;arr:array[26] of char;
	k,x,flag,ans:char;a,b:array[2] of integer;

begin
	read(n);
	left := 0;
	right := n;

	for i := 0 to right do
		read(arr);
end.
